# Репомікс проєкту VIP QA

**Дата оновлення:** 09.09.2026  
**Репозиторій:** [dtrefilova-cyber/vip_qa](https://github.com/dtrefilova-cyber/vip_qa)  
**Локальна копія:** `C:\Проєкти\vip_qa`  
**Деплой:** Streamlit Cloud з гілки `main`, файл входу `streamlit_app.py`  
**Локальний запуск:** `streamlit run app.py`

Актуальний `origin/main`: `20978ed` — *fix: write score points into RESULTS RESULT column* (31.08.2026).

**Відкриті feature-гілки (ще не в `main`):**

| Гілка | Що додає |
| --- | --- |
| `feature/vip-friendly-results-sheet` | запис VIP Friendly у аркуш **RESULTS FRIENDLY** тієї ж `VIP_SHORT_SHEET_ID` |
| `feature/vip-short-manual-results-sheet` | додатковий запис коротких у **ручну колонкову** трекінг-таблицю (окремий spreadsheet) |

---

## Що це за продукт

Streamlit-застосунок для QA відділу **VIP**: аналіз двох типів дзвінків за **бальними рубриками**.

| Тип | Ключ | Макс. бал | Rubric |
| --- | --- | --- | --- |
| **Короткий 90 сек** | `vip_short_90s` | **30** | `v2_90sec` |
| **VIP Friendly (2-й дзвінок)** | `vip_friendly` | **60** | `v1_friendly_ease10` |

**Вердикт GREEN/RED більше не є основною моделлю** для нових аналізів. GPT витягує лише типізовані факти; бали рахує Python. Для короткого критичні помилки зі **зливом** обнуляють дзвінок (0/30).

Структура файлів як у [QA-10](https://github.com/dtrefilova-cyber/QA-10): `app.py` / `streamlit_app.py`, `upload_cards.py`, `app_vip.py`, `vip_ui.py`, `vip_archive.py`, chrome/тема.

---

## Навігація

Сайдбар (AI Quality VIP), `ui_theme.NAV_ITEMS`:

1. **Короткі дзвінки** — головна (`app.py` / `streamlit_app.py`)
2. **VIP Friendly (2-й дзвінок)** — `pages/VIP_Friendly.py`
3. Дашборди
4. Менеджери
5. Гайди
6. Глосарій
7. Налаштування
8. Довідка

Профілі QA внизу сайдбара. Перемикач ☀ Light / 🌙 Dark у сайдбарі (сесія).  
☰-кнопка сайдбару зліва (`#aiq-sidebar-toggle`, `left: 12px`).  
`showSidebarNavigation = false` — кастомне меню.

Головна в Cloud стартує з `streamlit_app.py`, локально — з `app.py`. `chrome.home_page_path()` підставляє реальний main-файл у `st.page_link`.

---

## Сторінки дзвінків (Короткі + Friendly)

Однакова структура форми; відрізняються `call_type`, підпис і місце запису в Sheets.

### Підписи

- **Короткі:** «Таймінг — 30–90 с · Максимальний бал за дзвінок — 30»
- **Friendly:** «Обираємо дзвінки за коментарем (не спираємось на таймінги) · Максимальний бал за дзвінок — 60»

### Лічильники (за датою дзвінка, фільтр по типу)

З `vip_short_call_logs`:

1. **Всього дзвінків**
2. **Середній бал** (напр. `24/30` або `48/60`) — не %
3. **Опрацьовано сьогодні**

### Форма завантаження

- Стартово **2** картки, сітка по **2** в ряд, **+ Додати дзвінки** без ліміту
- Поля: посилання, проєкт, менеджер проєкту, ID, дата, бонус, важливе, коментар, галочки (не сервісний / дозвіл ТЛ)
- Результат бала + розбивка критеріїв — **в тій самій картці** (без 🔴/🟢)

### Архів

За дату перевірки, фільтр `call_type`. 3 картки на сторінку + пагінація.

---

## Як працює аналіз одного дзвінка

1. QA заповнює картку.
2. Deepgram транскрибує аудіо.
3. GPT витягує факти (`extract_vip_short_90s_facts` / `extract_vip_friendly_facts`).
4. Скоринг кодом (`score_vip_short_call` / `score_vip_friendly_call`).
5. Запис у Google Sheets (див. нижче).
6. Supabase `vip_short_call_logs` (`call_type`, `total_score`, `percent`, `criteria_*`, `verdict='scored'`).

Модель за замовчуванням: `gpt-5.4-mini` (секрет `OPENAI_MODEL`).

### Критерії короткого (макс. 30)

Контакт (5) + злив/утримання (10) + критичний гейт зі зливом + P.R.E.P. (10) + завершення (5).

### Критерії Friendly (макс. 60)

Контакт (5) + френдлі (7,5) + індивідуальний підхід (7,5) + заклик до гри (10) + бонуси (5) + P.R.E.P. (10) + завершення (5) + невимушеність (**10**).

`FORBIDDEN_WORDS` у коді є, але **не підключені** до скорингу цих двох типів.

---

## Дані · Google Sheets

### 1) Операційна таблиця VIP (`VIP_SHORT_SHEET_ID`)

ID: `1ww7dFbI8Gw7Ji96ssgSKBtlvv739xrAL9bNTfCiWqOc`

| Аркуш | Призначення |
| --- | --- |
| `MANAGERS` | PROJECT / TL / MANAGER |
| `RESULTS` | **Короткі** (рядковий A–G): RESULT = `{total}/{max}` (напр. `24/30`), без заливки GREEN/RED |
| `RESULTS FRIENDLY` | **Friendly** (рядковий A–G): RESULT = `{total}/{max} ({percent}%)` — *гілка `feature/vip-friendly-results-sheet`* |

Presence QA: окремий `LOG_SHEET_ID` → `ACTIVE_USERS`.

### 2) Ручна трекінг-таблиця коротких (`VIP_SHORT_MANUAL_TRACKING_SHEET_ID`)

ID: `1sse7mivevTjwn5PE8o1Nwb26_9PTKXeFrrrTygCL2x0`  
Аркуш: `RESULTS`  
*Гілка `feature/vip-short-manual-results-sheet` — паралельно з операційним RESULTS, не замінює його.*

**Формат — колонковий** (кожен дзвінок = нова колонка, з **C**; B уже зайнята):

| Рядок | Що пише код |
| --- | --- |
| 1 | Дата дзвінка |
| 2 | ID гравця |
| 3 | **VIP-менеджер** дзвінка (`ret_manager`) |
| 4 | Дата прослуховування (дата перевірки з UI) |
| 5 | Встановлення контакту (бал) |
| 6 | Робота зі зливом / утримання (бал) |
| 7 | Критичний гейт: порожньо або `критична` |
| 8 | P.R.E.P. (бал) |
| 9 | Завершення (бал) |

**Блок коментарів** з рядка **13**: A = ID, B = комент (при критичному зливі — `0/30` + причина).  
При критичному зливі бальні рядки 5/6/8/9 **не** заповнюються нулями.  
Помилка запису сюди — некритична (warning у UI), основний флоу не ламає.

Функції: `write_vip_short_manual_tracking`, `build_manual_tracking_column_values` у `google_sheets.py`.  
Тести: `tests/test_manual_tracking_sheet.py`.

---

## Дані · Supabase

Таблиця `vip_short_call_logs` (обидва типи). Нові колонки: `call_type`, `rubric_version`, `criteria_facts`, `criteria_scores`, `total_score`, `max_score`, `percent`, `is_critical_fail`.  
SQL: `supabase_migration_vip_scoring_v2.sql`. Історичні GREEN/RED не мігрують.

---

## Стек і секрети

Python, Streamlit, OpenAI, Deepgram, Google Sheets, Supabase, Plotly, Pydantic, pytest.

Обов’язкові: `DEEPGRAM_API_KEY`, `OPENAI_API_KEY`, `[gcp_service_account]` (доступ і до операційної, і до ручної трекінг-таблиці).  
Опційно: Supabase URL/keys; `OPENAI_MODEL`, `OPENAI_TRANSCRIPT_MODEL`.

Локально: `.streamlit/secrets.toml` (не в git).

---

## Карта файлів

| Файл | Роль |
| --- | --- |
| `app.py` / `streamlit_app.py` | Точки входу → Короткі |
| `pages/VIP_Friendly.py` | Сторінка Friendly |
| `vip_ui.py` | Шапка, дата перевірки |
| `app_vip.py` | Оркестрація аналізу, Sheets + ручний трекінг |
| `upload_cards.py` | Картки внесення |
| `vip_archive.py` | Лічильники й архів |
| `core/vip_scoring_common.py` | Спільні критерії |
| `core/vip_short_scoring.py` | Короткий 90 сек |
| `core/vip_friendly_scoring.py` | VIP Friendly |
| `vip_short_ai_assistant.py` | GPT-факти |
| `prompts_vip_*.py` | Промпти |
| `guides_content.py` | Текст гайдів |
| `constants.py` | Типи, sheet IDs, max scores |
| `google_sheets.py` | MANAGERS, RESULTS, RESULTS FRIENDLY, ручний трекінг |
| `supabase_logger.py` | `vip_short_call_logs` |
| `tests/test_vip_scoring.py` | Скоринг |
| `tests/test_google_sheets_friendly.py` | Формат Friendly Sheets *(feature)* |
| `tests/test_manual_tracking_sheet.py` | Колонковий трекінг коротких *(feature)* |
| `pages/` | Дашборди, Менеджери, Гайди, Глосарій, Налаштування, Довідка |

---

## Що змінилось (серпень–вересень 2026)

- Red/green → бальні рубрики; тип **VIP Friendly** + окрема сторінка.
- Лічильники: всього / середній бал / сьогодні.
- Операційний RESULTS: бали замість заливки.
- Невимушеність: макс. **10** (Friendly **60**).
- Термін **злив**; спрощені Гайди.
- **Friendly → RESULTS FRIENDLY** (feature-гілка).
- **Короткі → ручна колонкова трекінг-таблиця** (feature-гілка; рядок 3 = VIP-менеджер).

---

## Чого в цьому репо немає

Retention, Return, типи «Знайомство» / «Сервісні» як окремі продукти, Behavioral Signals, черга pending logs, KB GitHub loader.
