# Репомікс проєкту VIP QA

**Дата оновлення:** 09.09.2026  
**Репозиторій:** [dtrefilova-cyber/vip_qa](https://github.com/dtrefilova-cyber/vip_qa)  
**Локальна копія:** `C:\Проєкти\vip_qa`  
**Деплой:** Streamlit Cloud з гілки `main`, файл входу `streamlit_app.py`  
**Локальний запуск:** `streamlit run app.py`

Актуальний `origin/main`: `20978ed` — *fix: write score points into RESULTS RESULT column* (31.08.2026).  
Гілка `feature/vip-friendly-results-sheet` (поверх main): запис VIP Friendly у аркуш **RESULTS FRIENDLY** — ще не змерджена в `main` (чекає PR/merge).

---

## Що це за продукт

Streamlit-застосунок для QA відділу **VIP**: аналіз двох типів дзвінків за **бальними рубриками**.

| Тип | Ключ | Макс. бал | Rubric |
| --- | --- | --- | --- |
| **Короткий 90 сек** | `vip_short_90s` | **30** | `v2_90sec` |
| **VIP Friendly (2-й дзвінок)** | `vip_friendly` | **60** | `v1_friendly_ease10` |

**Вердикт GREEN/RED більше не є основною моделлю** для нових аналізів. GPT витягує лише типізовані факти; бали рахує Python. Для короткого критичні помилки зі **зливом** обнуляють дзвінок (0/30).

Структура файлів як у [QA-10](https://github.com/dtrefilova-cyber/QA-10): `app.py` / `streamlit_app.py`, `upload_cards.py`, `app_vip.py`, `vip_ui.py`, `vip_archive.py`, chrome/тема.

---

## Навігація

Сайдбар (AI Quality VIP), `ui_theme.NAV_ITEMS`:

1. **Короткі дзвінки** — головна (`app.py` / `streamlit_app.py`)
2. **VIP Friendly (2-й дзвінок)** — `pages/VIP_Friendly.py`
3. Дашборди
4. Менеджери
5. Гайди
6. Глосарій
7. Налаштування
8. Довідка

Профілі QA внизу сайдбара. Перемикач ☀ Light / 🌙 Dark у сайдбарі (сесія).  
☰-кнопка сайдбару зліва (`#aiq-sidebar-toggle`, `left: 12px`).  
`showSidebarNavigation = false` — кастомне меню, не дефолтне Streamlit.

Головна в Cloud стартує з `streamlit_app.py`, локально — з `app.py`. `chrome.home_page_path()` підставляє реальний main-файл у `st.page_link`.

---

## Сторінки дзвінків (Короткі + Friendly)

Однакова структура форми; відрізняються `call_type`, підпис і аркуш Sheets.

### Підписи

- **Короткі:** «Таймінг — 30–90 с · Максимальний бал за дзвінок — 30»
- **Friendly:** «Обираємо дзвінки за коментарем (не спираємось на таймінги) · Максимальний бал за дзвінок — 60»

### Лічильники (за датою дзвінка, фільтр по типу)

З `vip_short_call_logs`:

1. **Всього дзвінків**
2. **Середній бал** (напр. `24/30` або `48/60`) — не %
3. **Опрацьовано сьогодні**

Без ключів Supabase аналіз працює, лічильники показують 0.

### Форма завантаження

- Стартово **2** картки, сітка по **2** в ряд, **+ Додати дзвінки** без ліміту
- Статуси: НОВИЙ / ГОТОВИЙ / АНАЛІЗУЄТЬСЯ / ПРОАНАЛІЗОВАНО / ПОМИЛКА

Поля картки (однакові для обох типів):

- Посилання на дзвінок
- **Проєкт** (select з MANAGERS)
- **Менеджер проєкту** (фільтр за проєктом; ТЛ з рядка менеджера)
- ID клієнта
- Дата дзвінка
- Бонус (нараховано вірно / невірно / не нараховано) — інформаційно
- Важливе
- Коментар до дзвінка
- ☐ Попередній дзвінок не був сервісним
- ☐ Є дозвіл ТЛ на дзвінок без структури
- **Аналіз цього дзвінка**

Обов’язкові: посилання, проєкт, менеджер, ID, дата.  
Результат бала + розбивка критеріїв — **в тій самій картці** (без 🔴/🟢).

### Архів

За дату перевірки, фільтр `call_type`. 3 картки на сторінку + пагінація. Бейдж бала, найслабший критерій / критична помилка.

---

## Як працює аналіз одного дзвінка

1. QA заповнює картку.
2. Deepgram транскрибує аудіо.
3. GPT витягує факти:
   - Короткий → `extract_vip_short_90s_facts` (`prompts_vip_short.py`, cache `vip_short90s_r1_20260831`)
   - Friendly → `extract_vip_friendly_facts` (`prompts_vip_friendly.py`, cache `vip_friendly_r1_20260831`)
4. Скоринг кодом:
   - `score_vip_short_call` / `score_vip_friendly_call`
5. Google Sheets:
   - Короткий → аркуш **RESULTS**, колонка RESULT = `{total}/{max}` (напр. `24/30`), без заливки GREEN/RED
   - Friendly → аркуш **RESULTS FRIENDLY**, RESULT = `{total}/{max} ({percent}%)` (на гілці `feature/vip-friendly-results-sheet`)
6. Supabase `vip_short_call_logs` з `call_type`, `total_score`, `percent`, `criteria_*`, `verdict='scored'`

Модель за замовчуванням: `gpt-5.4-mini` (секрет `OPENAI_MODEL`).

Ланцюг: entry → `vip_ui.py` → `app_vip.py` → `upload_cards.py` + `utils.py` + `core/` + Sheets/Supabase.

### Спільні критерії (`core/vip_scoring_common.py`)

- Встановлення контакту — 5  
- P.R.E.P. — 10 (найгірше заперечення; без заперечень — автомакс)  
- Завершення — 5 (+ автозалік, якщо клієнт кинув першим)

### Короткий 90 сек (макс. 30)

Контакт + робота зі **зливом** (10) + P.R.E.P. + завершення.  
Критичний блок зі зливом → **0/30**.

### VIP Friendly (макс. 60)

Контакт + розвиток френдлі (7,5) + індивідуальний підхід (7,5) + заклик до гри (10) + бонуси (5) + P.R.E.P. + завершення + **невимушеність (10)**.

`FORBIDDEN_WORDS` у `constants.py` лишаються в коді, але **не підключені** до нового скорингу цих двох типів (свідоме рішення продукту).

---

## Інші сторінки

| Сторінка | Що робить |
| --- | --- |
| Дашборди | KPI середній %, критичні, розподіл по типах / менеджерах з `vip_short_call_logs` |
| Менеджери | Довідник PROJECT / TL / MANAGER з MANAGERS |
| Гайди | Два таби (Короткий / Friendly), критерії в expander з `guides_content.py` |
| Глосарій | Терміни бального скорингу (злив, P.R.E.P., френдлі, невимушеність…) |
| Налаштування | Кеш, debug, присутність QA |
| Довідка | Короткий опис розділів |

---

## Дані

**Google Sheet** (`VIP_SHORT_SHEET_ID` = `1ww7dFbI8Gw7Ji96ssgSKBtlvv739xrAL9bNTfCiWqOc`):

| Аркуш | Призначення |
| --- | --- |
| `MANAGERS` | PROJECT / TL / MANAGER |
| `RESULTS` | Короткі: A–G, RESULT = бали `{total}/{max}` |
| `RESULTS FRIENDLY` | Friendly: A–G, RESULT = `{total}/{max} ({percent}%)` + розбивка в Коментарі *(гілка feature, після merge в main)* |
| Presence | окремий `LOG_SHEET_ID` → `ACTIVE_USERS` |

**Supabase** — `vip_short_call_logs` (обидва типи). Нові колонки (nullable): `call_type`, `rubric_version`, `criteria_facts`, `criteria_scores`, `total_score`, `max_score`, `percent`, `is_critical_fail`.  
SQL: `supabase_migration_vip_scoring_v2.sql`. Історичні GREEN/RED рядки не мігрують.

---

## Стек і секрети

Python, Streamlit, OpenAI, Deepgram, Google Sheets, Supabase, Plotly, Pydantic, pytest.

Обов’язкові: `DEEPGRAM_API_KEY`, `OPENAI_API_KEY`, `[gcp_service_account]`.  
Опційно: `SUPABASE_URL` + service/secret/publishable key; `OPENAI_MODEL`, `OPENAI_TRANSCRIPT_MODEL`.

Локально: `.streamlit/secrets.toml` (не в git). Хмара: App settings → Secrets.

---

## Карта файлів

| Файл | Роль |
| --- | --- |
| `app.py` / `streamlit_app.py` | Точки входу → Короткі |
| `pages/VIP_Friendly.py` | Сторінка Friendly |
| `vip_ui.py` | Шапка, дата перевірки, мета сторінок |
| `app_vip.py` | Оркестрація аналізу, маршрутизація Sheets |
| `upload_cards.py` | Картки внесення (параметр `call_type`) |
| `vip_archive.py` | Лічильники й архів |
| `core/vip_scoring_common.py` | Спільні критерії + `ScoringResult` |
| `core/vip_short_scoring.py` | Короткий 90 сек |
| `core/vip_friendly_scoring.py` | VIP Friendly |
| `vip_short_ai_assistant.py` | GPT-екстракція фактів (обидва типи) |
| `prompts_vip_short.py` / `prompts_vip_friendly.py` | Промпти |
| `guides_content.py` | Текст гайдів |
| `constants.py` | Типи, max scores, sheet ID, поля карток |
| `google_sheets.py` | MANAGERS, RESULTS, RESULTS FRIENDLY, presence |
| `supabase_logger.py` | Запис у `vip_short_call_logs` |
| `chrome.py` / `ui_theme.py` / `aiq_tokens.css` | Сайдбар, тема |
| `tests/test_vip_scoring.py` | Unit-тести скорингу |
| `tests/test_google_sheets_friendly.py` | Формат RESULT/Коментар Friendly |
| `pages/` | Дашборди, Менеджери, Гайди, Глосарій, Налаштування, Довідка |

---

## Що змінилось у серпні 2026 (коротко)

- Red/green для короткого **замінено** бальною рубрикою; додано тип **VIP Friendly**.
- Окрема сторінка Friendly у сайдбарі; спільна форма карток.
- Лічильники: всього / середній бал / сьогодні.
- Sheets: бали в RESULT замість заливки; Friendly → окремий аркуш (feature-гілка).
- Невимушеність: макс. **10** (загальний Friendly **60**).
- Термін **злив** (не «слив»); спрощені Гайди.

---

## Чого в цьому репо немає

Retention, Return, типи «Знайомство» / «Сервісні» як окремі продукти, Behavioral Signals, черга pending logs, KB GitHub loader. Зміни з `qa-deterministic` і QA-10 сюди самі не підтягуються.
